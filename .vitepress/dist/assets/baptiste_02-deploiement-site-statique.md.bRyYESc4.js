import{_ as e,c as s,o as t,a2 as i}from"./chunks/framework.BXMoTSpH.js";const v=JSON.parse(`{"title":"Déploiement d'un site statique","description":"","frontmatter":{},"headers":[],"relativePath":"baptiste/02-deploiement-site-statique.md","filePath":"baptiste/02-deploiement-site-statique.md"}`),a={name:"baptiste/02-deploiement-site-statique.md"},n=i(`<h1 id="deploiement-d-un-site-statique" tabindex="-1">Déploiement d&#39;un site statique <a class="header-anchor" href="#deploiement-d-un-site-statique" aria-label="Permalink to &quot;Déploiement d&#39;un site statique&quot;">​</a></h1><p>On a vu que selon l&#39;application à déployer, les étapes à suivre ne sont pas forcément identiques.</p><p>On va commencer par le plus simple : déployer un <strong>site statique</strong> ! Et ça tombe bien, on en a un sous la main : le front-end de l&#39;application Pomodor&#39;O.</p><p>Pour déployer un site statique, qui ne comporte que du code HTML, CSS et JS, il suffit de déplacer nos fichiers dans le dossier <code>/var/www/html</code> du serveur.</p><blockquote><p>Déplacer des fichiers sur un serveur ? On fait ça comment ?</p></blockquote><p>Plusieurs solutions nous permettent de copier des fichiers sur un serveur distant, sans y avoir d&#39;accès physique.</p><h2 id="via-le-terminal-avec-scp" tabindex="-1">Via le terminal, avec SCP <a class="header-anchor" href="#via-le-terminal-avec-scp" aria-label="Permalink to &quot;Via le terminal, avec SCP&quot;">​</a></h2><p>On connait déjà la commande <code>cp</code>, qui permet de copier/coller des fichiers en ligne de commande sur un système GNU/Linux.</p><p>On a également découvert tout à l&#39;heure le protocole <code>ssh</code>, qui permet de se connecter à un serveur distant et de l&#39;administrer en ligne de commande.</p><p>Le protocole <strong>SCP</strong> est la combinaison des deux : comme une copie avec <code>cp</code>, mais réalisée entre deux machines distantes à travers le réseau. SCP est l&#39;acronyme de <strong>Secure CoPy</strong>.</p><p>L&#39;avantage de ce protocole ? L&#39;utilitaire permettant de faire du SCP est disponible sur <strong>n&#39;importe quel serveur sur lequel vous pouvez vous connecter en SSH</strong>, il n&#39;y a rien à installer.</p><p>Pour envoyer un fichier <code>index.html</code> depuis votre téléporteur vers votre serveur, dans le dossier <code>/var/www/html</code>, vous pourriez lancer la commande SCP suivante :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./index.html</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> student@PSEUDOGH-server.eddi.cloud:/var/www/html/index.html</span></span></code></pre></div><p>⚠️ J&#39;ai dit &quot;pourriez&quot;, ne lancez pas la commande ci-dessus !</p><p>Pour déployer le front-end de notre application en SCP, lancez la commande suivante <strong>depuis votre téléporteur et depuis la racine de ce dépôt</strong> :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> front/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> student@PSEUDOGH-server.eddi.cloud:/var/www/html/</span></span></code></pre></div><p>Rendez-vous à l&#39;adresse <a href="http://PSEUDOGH-server.eddi.cloud" target="_blank" rel="noreferrer">http://PSEUDOGH-server.eddi.cloud</a> (remplacez <code>PSEUDOGH</code> par votre nom d&#39;utilisateur github). Vous devriez-voir le front-end de notre application, enfin plus exactement un message <strong>impossible de se connecter à l&#39;API</strong>.</p><p>Bien que cette solution fonctionne et soit sécurisée, elle est un peu rudimentaire et on va voir d&#39;autres méthodes plus modernes.</p><h2 id="ftp-sftp" tabindex="-1">FTP &amp; SFTP <a class="header-anchor" href="#ftp-sftp" aria-label="Permalink to &quot;FTP &amp; SFTP&quot;">​</a></h2><blockquote><p>Plus moderne ? Et tu nous parle de FTP ? 😱</p></blockquote><p>Certains d&#39;entre-vous le connaissent peut-être déjà : le protocole <strong>FTP (File Transfer Protocol)</strong> est, comme son nom l&#39;indique, un protocole dédié au transfert de fichiers.</p><p>Ce protocole n&#39;est plus vraiment utilisé de nos jours, mais son successeur, <strong>SFTP (SSH File Transfer Protocol)</strong> est encore utilisé par certains hébergeurs ! (c&#39;est même la seule solution proposée dans certains cas)</p><p>Pour utiliser FTP ou SFTP, il faut un logiciel coté serveur et un autre coté client. Coté serveur, nous allons utiliser un script pour installer et configurer automatiquement le logiciel ProFTPd (sa configuration est laborieuse, plutôt réservée à des administrateurs système de métier !).</p><p><strong>Sur votre VM serveur</strong>, lancez les commandes suivantes pour installer ProFTPd :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wget</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://gist.githubusercontent.com/bdelphin/b5fbddbe2d86789a920dd2a9d9e5c4ce/raw/29d30954fc0575033fa92c04ca0feafadfa5ee1d/install-ftp.sh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +x</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./install-ftp.sh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./install-ftp.sh</span></span></code></pre></div><p>L&#39;installation du serveur SFTP est terminée 🎉 Conservez les informations données à la fin de l&#39;installation, vous en aurez besoin pour vous connecter coté client.</p><p>⚠️ Attention, nous venons de télécharger un script depuis Internet et nous l&#39;avons lancé sur notre serveur sans vérifier son contenu au préalable. Cette pratique est à éviter : le script pourrait par exemple ouvrir une <strong>backdoor</strong> sur notre serveur. Le script en question est parfaitement sûr dans le cas présent.</p><p><strong>Sur votre téléporteur</strong>, lancez la commande suivante pour installer le client FTP FileZilla :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> filezilla</span></span></code></pre></div><p>Vous pouvez ensuite ouvrir FileZilla et vous connecter à votre serveur. Ce logiciel utilise une interface de type &quot;commander&quot;, avec deux explorateurs de fichiers ouverts simultanément à gauche et à droite du logiciel. À gauche, vos dossiers locaux, sur votre téléporteur; à droite, les dossiers distants, sur le serveur.</p><p>Pour copier les fichiers vers ou depuis le serveur, il suffit de faire un glisser/déposer. On a déjà déployé le front via SCP tout à l&#39;heure, mais on a oublié la favicon !</p><p>Faites glisser le fichier <code>favicon.ico</code> dans le dossier <code>/var/www/html</code> du serveur.</p><p>Actualisez la page ouverte tout à l&#39;heure dans votre navigateur (avec <code>Ctrl+Shift+R</code>) : c&#39;est bon, la favicon est en place.</p><p>C&#39;est un peu mieux que SCP, mais toujours trop rudimentaire comme façon de faire : par exemple, il est <strong>difficile de revenir en arrière en cas de problème</strong> avec le code déployé !</p><h2 id="git" tabindex="-1">Git <a class="header-anchor" href="#git" aria-label="Permalink to &quot;Git&quot;">​</a></h2><p>De nos jours, la plupart des applications sont déployées grâce à Git. Contrairement à un déploiement via SCP ou FTP/SFTP, il sera facile de revenir en arrière en cas de problème.</p><p>Nous allons donc cloner notre dépôt sur le serveur, mais avant ça, il faut qu&#39;on <strong>configure une clé SSH</strong> pour pouvoir nous connecter à notre compte Github depuis le serveur.</p><p>Lancez la commande suivante, <strong>remplacez <code>your_email@example.com</code> par l&#39;adresse email utilisée sur votre compte github</strong> :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ssh-keygen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ed25519</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -C</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;your_email@example.com&quot;</span></span></code></pre></div><p>Cette commande va nous demander dans quel fichier sauvegarder la clé (Enter a file in which to save the key), appuyez sur Entrée pour valider l&#39;emplacement par défaut.</p><p>La commande va ensuite nous demander si on souhaite mettre un mot de passe sur la clé (Enter passphrase), appuyez sur Entrée pour ne pas mettre de mot de passe.</p><p>Une fois la clé générée, il faut l&#39;ajouter sur notre compte github. Affichez la clé publique avec la commande suivante :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.ssh/id_ed25519.pub</span></span></code></pre></div><p>Copiez la clé et l&#39;adresse email.</p><p>Rendez-vous dans les <a href="https://github.com/settings/keys" target="_blank" rel="noreferrer">paramètres des clés SSH et GPG de votre compte github</a>, puis cliquez sur le bouton vert <code>New SSH key</code>.</p><p>Donnez un nom à cette clé dans le champ <code>Title</code>, puis collez la clé publique copiée précédemment dans le champ <code>Key</code>. Laissez <code>Key type</code> sur le réglage &quot;Authentication Key&quot;. Enregistrez la clé en cliquant sur le bouton vert <code>Add SSH key</code>.</p><p>À ce stade, on peut maintenant cloner le dépôt dans le dossier <code>/var/www/html</code> avec la commande suivante (remplacez <code>PROMO</code> par le nom de votre promotion) :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/www/html</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> git@github.com:O-clock-PROMO/S08-PHP-Pomodor-O.git</span></span></code></pre></div><p>⚠️ Problème : notre front-end est dans le dossier <code>front/</code>, pas dans le dossier <code>/var/www/html</code> directement ... il faut donc se rendre sur <a href="http://bdelphin-server.eddi.cloud/S08-PHP-Pomodor-O/front/" target="_blank" rel="noreferrer">http://bdelphin-server.eddi.cloud/S08-PHP-Pomodor-O/front/</a> pour y accéder. Pas génial non ?</p><p>⚠️ Il y a également un problème de sécurité ! Le dossier <code>.git</code> de notre dépôt se retrouve exposé par le serveur web Apache 😱 (pour rappel, ce dossier contient l&#39;historique complet des commits du dépôt, l&#39;adresse du dépôt sur github, etc.)</p><blockquote><p>Pas grave, on a qu&#39;à déplacer les fichiers et supprimer ce dossier <code>.git</code> ?</p></blockquote><p>On pourrait effectivement utiliser des commandes <code>mv</code> et <code>rm</code> / <code>rmdir</code> pour déplacer les fichiers au bon endroit et supprimer les informations sensibles, mais dans ce cas-là on perd le principal intéret de déployer via Git : on ne pourra plus revenir en arrière en cas de problème !</p><p>La solution ? On va mettre en place un <strong>virtual host Apache</strong>.</p><p>Il nous reste également le back-end de l&#39;application à déployer, et ça tombe bien, on aura besoin des <strong>virtual hosts</strong> pour ça aussi.</p><p>Avant de passer à la suite, supprimons les fichiers de notre front-end que nous avons déployé via SCP et SFTP :</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /var/www/html</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> assets/</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> favicon.ico</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> index.html</span></span></code></pre></div><p>Et pour la suite, c&#39;est par <a href="./03-virtual-hosts.html">ici</a> !</p>`,57),r=[n];function o(l,p,d,c,u,h){return t(),s("div",null,r)}const g=e(a,[["render",o]]);export{v as __pageData,g as default};
